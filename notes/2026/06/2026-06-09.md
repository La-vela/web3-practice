# Recipe Contest (2026-06-09)

## Sepolia Testnet Deployment 순차도

### 1. MetaMask 테스트 계정 생성

### 2. Alchemy Sepolia RPC 발급

### 3. `.env` 설정

```env
PRIVATE_KEY=...
RPC_URL=...
```

### 4. hardhat.config.ts 네트워크 설정

```ts
sepolia: {
  url: process.env.RPC_URL!,
  accounts: [process.env.PRIVATE_KEY!]
}
```

### 5. Sepolia ETH 수령

### 6. 컨트랙트 배포

```bash
npx hardhat run scripts/deploy.ts --network sepolia
```

결과:

```text
RecipeContest deployed to:
0x...
```

## Next Step

* Etherscan Verify
* MetaMask 연동
* Unity 연결
