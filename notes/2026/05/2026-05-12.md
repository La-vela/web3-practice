# 2026-05-12 | HackQuest + Gemini learn Day 48

**Time:** 30min

---

## Learn


### signature
```
mySignature(uint256,bool)
```

```
bytes4(keccak256("func(pram1, parm2)"))
```


### Function Selector
솔리디티 소스 코드 내에서 함수를 구분하기 위해 만들어진 고유산 식별자
- 함수 실행 요청시, 그 함수가 정확히 어떤 함수인지 해시를 돌린 뒤 그 결과물의 앞에 4바이트(8글자)만 사용한 것이 'Function Selector'
```
unction transfer(address _to, uint256 _value) public returns (bool)

// Get the function selector of below function manually and assign to selector.
function thisIsMyFunction(bytes a, uint256 b) public {..} 
bytes4 selector; 
selector = bytes4(keccak256("thisIsMyFunction(bytes,uint256)"));
 
 
```

### abi

```
abi.encodeWithSignature("myFunction(uint256,string)", 123, "Hello");
```
## Hackathon (NusaPay)

### supportedExchangeRates (환율 계산)
thirdPartyController.ts의  parseConversionRequest 함수
- 교차 환율 계산식 예) 10 ETH to IDRX → 10 × (15000 ÷ 2500) = 60 IDRX
- 부동 소수점 예) 0.1 + 0.2 = 0.300000000004 같은 오류 방지
```
// supportedExchangeRates: 사전에 정의된 환율 테이블 (예: { ETH: 2500, USDC: 1, IDRX: 15000 })
const calculateConversion = (request: ConversionRequest, rates: Record<string, number>) => {
const { amount, fromCurrency, toCurrency } = request;

// 1. 소수점 안전 처리 (부동소수점 오류 방지)
const fromRate = rates[fromCurrency];
const toRate = rates[toCurrency];
if (!fromRate || !toRate) {
    throw new Error("Exchange rate not available");
}

// 2. 교차 환율 계산: amount * (toRate / fromRate)
const convertedAmount = amount * (toRate / fromRate);

// 3. 소수점 8자리로 반올림 (암호화폐 표준)
const finalAmount = Math.floor(convertedAmount * 1e8) / 1e8;
return {
    original: request,
    convertedAmount: finalAmount,
    rateUsed: toRate / fromRate,
    timestamp: Date.now()
};
```
### parseConversionRequest (함수환전 처리 서비스)
```
// 1. 유저의 입력을 받아서 처리하는 메인 컨트롤러 함수
const handleUserMessage = async (userInput: string) => {
    try {
            // 지난번에 공부한 유효성 검사 및 데이터 파싱
            const requestData = parseConversionRequest(userInput);
            if (!requestData) {
                return "죄송해요, 이해하지 못했습니다. (예: 10 ETH to IDRX)";
            }

            // 위에서 파싱된 결과를 바로 계산 엔진으로 전달
            const result = calculateConversion(requestData, currentRates);
            return `환전 결과: ${result.convertedAmount} ${result.original.toCurrency} (적용 환율: ${result.rateUsed})`;
        } catch (error) {
            return `에러 발생: ${error.message}`;
        }
};
```


---

## Future Objective
한 개의 해커톤 프로젝트 찍먹 분석하기 (3)
- 데이터 무결성은 기본