# 2026-05-20 | HackQuest + Gemini learn Day 55

**Time:** 15min

---

## Guide Project 3


### mint the initial supply of tokens
1. 상태 변수 선언: 지갑별 잔액을 관리할 mapping
2. 전체 발행량을 추적할 totalSupply, 권한을 제어할 owner 설정 
3. 권한 관리(Access Control): constructor에서 owner = msg.sender를 통해 컨트랙트를 배포한 사람 지정
4. 발행 로직(Mint Logic): require 문을 써서 오직 주인만 토큰을 발행할 수 있게 제한하고, 잔액과 총 발행량을 업데이트

```
pragma solidity 0.8.17;

contract MyToken {
    //mapping used to store the balance correspording to each address
    mapping(address => uint256) private balances;

    //A uint256 variable is used to store the total supply of the token. It is defined as public and can be queried by anyone.
    uint256 public totalSupply;

    //An address variable is used store the issuer of this token. This is used for some permission control.
    address private owner;

    cocnstructor(uint256 initialSupply) {
        mint(msg.sender, initialSupply);

        owner = msg.sender;
    }

    // function used to mint tokens
    function mint(address recipient, uint256 amount) public {
        //Permission control is implemented
        require(msg.sender == owner, "Only the owner can perform this action");
        balances[recipient] += amount;
        totalSupply += amount;
    }

}
```

이후
Deploy on Remix VM으로 배포 (RemixIDE Metamask)
 ptimize&runs=200&evmVersion&version=soljson-v0.8.34+commit.80d5c536.js

---

## Future Objective
require 작동 원리로 주인만 사용하게 해주는 방법