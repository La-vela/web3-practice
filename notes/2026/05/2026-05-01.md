# 2026-05-01 | HackQuest + Gemini learn Day 37

## Today's Learning Objectives

**Time:** 10min

---
d
## Learn


### Contract Variable (계약 변수)

정의하고 배포하고 상호작용
```
MyContract mycontract;

//The contract instance needs to be instantiated through new.
mycontract = new MyContract(); 

//It can also be instantiated by specifying an address.
MyContract mycontract = MyContract(contractAddress); 
```


DigitalAssetContract의 소유권을 새로운 주소로 이전
```
contract DigitalAssetContract {
  function transferOwnership(address newOwner) public {
    // ...
  }
}

executeTrade: 마켓플레이스에서 거래 실행 시 디지털 자산 소유권 이전
contract MarketplaceContract {
  DigitalAssetContract public digitalAsset;

  function executeTrade(address newOwner) public {
    digitalAsset.transferOwnership(newOwner);
  }
}
```

금액 지정 함수 호출
```
treasury.deposit(amount);
```



---

## Future Objective
