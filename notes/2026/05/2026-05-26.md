# 2026-05-26 | HackQuest + Gemini learn Day 60

**Time:** 10min

---


## Crypto kitty
- createKittyGen0 함수 0세대 고양이 생성
- keccak256를 사용하여 해시 생성
- keccak256 속 abi.encodePacked를 활용하여 고유 함수 생성
- constructor() ERC721 생성자 내 createKittyGen0()함수 호출
- 번식을 위해 고양이 한 쌍이 필요하므로 이 함수를 두 번 호출

```
pragma solidity 0.8.17;

import "@openzeppelin/contracts/token/ERC721/ERC721.sol";

contract SimpleCryptoKitties is ERC721 {
  uint256 public _tokenIdCounter = 1;
  ... // 이전 코드

  constructor() ERC721("SimpleCryptoKitties", "SCK") {
    createKittyGen0();
    createKittyGen0();
  }

  function createKittyGen0() private returns (uint256) {
    uint256 genes = uint256(
      keccak256(abi.encodePacked(block.timestamp, _tokenIdCounter))
    );

   return _createKitty(0, 0, 0, genes, msg.sender);
  }
}
```