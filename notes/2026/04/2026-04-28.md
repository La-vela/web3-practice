# 2026-04-29 | HackQuest + Gemini learn Day 34

## Today's Learning Objectives

**Time:** min

---

## Learn

### Ether payable
```
contract MyContract {
    function acceptPayment() public payable {
        // function body
    }
}
```

payable keyword
```
function depositFunds() public payble {
    // Code to execute when Ether is sent
}
```

### block.number
현재 블록의 순차 번호를 나타내는 솔리디티의 전역 변수
```
function processblock() public {
    uint currentBlock = block.number;
    // currentBlock now holds the number of the current block
}
```

### Example

이전에 작성한 코드처럼, 이더 전송과 잔액 확인
```
// SPDX-License-Identifier: GPL-3.0
pragma solidity ^0.8.17;

contract PayableExample{
    function depositFunds() public payble { }

    function getBalance() public view returns (uint) {
        return address(this).balance;
    }
}
```


---

## Future Objective
